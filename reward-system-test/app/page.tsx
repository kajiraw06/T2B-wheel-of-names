"use client"
import RewardSystem from './RewardSystem'

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      {/* ...existing code... */}
      {/* RewardSystem component removed as requested */}
      {/* ...existing code... */}
    </main>
  )
}